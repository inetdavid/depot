%tr{:id => ('current_item' if line_item == @current_item)}
  %td
    = succeed "&times;".html_safe do
      = line_item.quantity
  %td
    = line_item.product.title
  %td.item_price
    = number_to_currency(line_item.total_price)
